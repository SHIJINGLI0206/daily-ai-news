# Browser-safe public configuration only. Keep secrets in the future backend.
NEXT_PUBLIC_APP_NAME=AI Radar
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# GitHub Pages is static and will use the local seed data unless this points to a deployed API.
